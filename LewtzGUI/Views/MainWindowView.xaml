<UserControl x:Class="LewtzGUI.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vw="clr-namespace:LewtzGUI.Views"
        xmlns:vm="clr-namespace:LewtzGUI.ViewModel"
        mc:Ignorable="d"
        Height="525" Width="725">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="275"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
            <Button Command="{Binding AddItemRoller}" Width="auto" Height="auto">Add Hoard</Button>
            <Button Command="{Binding OnItemRollerRequestClose}" Width="auto" Height="auto">Remove Hoard</Button>
        </StackPanel>

        <ScrollViewer Grid.Column="0" Grid.Row="1" Height="auto" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ItemRollers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate xmlns:t="clr-namespace:LewtzGUI.ViewModel"
                                  DataType="{x:Type t:ItemRollerViewModel}">
                        <vw:HoardControl/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <GridSplitter Grid.Column="1" Grid.RowSpan="2" 
                      VerticalAlignment="Stretch" Width="2"
                      HorizontalAlignment="Center"
                      BorderBrush="DarkSlateGray" Background="Black"/>


        <TextBlock></TextBlock>
    </Grid>
</UserControl>
